package com.navigator.core.nav

/** High-level status of the active route, as reported by the navigation engine. */
enum class RouteStatus { UNKNOWN, ROUTING, ON_ROUTE, OFF_ROUTE, ARRIVED, NO_ROUTE }
